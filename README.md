# Limbus Helper

## Installation

Create directory [path/to/windower]/addons/LimbusHelper
Place LimbusHelper.lua inside [path/to/windower]/addons/LimbusHelper

If you wish to autoload the addon at startup, update [path/to/windower]/scripts/init.txt with the line "lua load limbushelper"
